#include <iostream>
#include <vector>

typedef unsigned int uint;

std::vector<int> LCG(uint n);

int main()
{
    uint n = 0;
    std::cin >> n;
}

std::vector<int> LCG(uint n) {

    return std::vector<int>();
}